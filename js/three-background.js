// Remove the entire Three.js background implementation
class ThreeBackground {
    constructor() {
        // Empty constructor
    }
}

// Initialize when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    // No initialization needed
}); 